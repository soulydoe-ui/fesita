<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Query installed launcher applications for AppLauncherService and Radio backend -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_MUSIC" />
        </intent>
        <intent>
            <action android:name="android.intent.action.RADIO_SEARCH" />
        </intent>
        <package android:name="com.android.fmradio" />
        <package android:name="com.caf.fmradio" />
        <package android:name="com.mediatek.fmradio" />
        <package android:name="com.syu.radio" />
        <package android:name="com.syu.fm" />
        <package android:name="com.microntek.radio" />
        <package android:name="com.ts.radio" />
        <intent>
            <action android:name="android.car.action.HVAC_STATUS" />
        </intent>
        <package android:name="com.syu.canbus" />
        <package android:name="com.ts.can" />
        <package android:name="com.microntek.controlinfo" />
        <package android:name="com.microntek.canbus" />
        <package android:name="com.microntek.hvac" />
        <package android:name="com.hiworld.canbus" />
        <package android:name="com.raise.canbus" />
        <package android:name="com.simplesoft.canbus" />
        <package android:name="com.szchoi.canbus" />
        <package android:name="com.android.car.hvac" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name="com.fiestast.launcher.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name="com.example.MainActivity"
            android:targetActivity="com.fiestast.launcher.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- NotificationListenerService for Phase 2 (MediaSession) & Phase 3 (Navigation Guidance) -->
        <service
            android:name="com.fiestast.launcher.android.notifications.LauncherNotificationListener"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Boot Receiver for safe startup initialization -->
        <receiver
            android:name="com.fiestast.launcher.android.receivers.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
